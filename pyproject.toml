[project]
name = "surveycontroller"
version = "3.2.0"
description = "SurveyController desktop application"
readme = "README.md"
requires-python = ">=3.13.14"
dependencies = [
    "beautifulsoup4>=4.15.0",
    "httpx>=0.27,<1",
    "keyring>=25,<26; sys_platform == 'darwin'",
    "packaging",
    "py-yaml12",
    "python-calamine",
]

[dependency-groups]
dev = [
    "openpyxl",
    "pyright>=1.1.410",
    "pytest",
    "pytest-asyncio",
    "pytest-cov",
    "ruff>=0.15.17",
]

[tool.uv]
package = false
default-groups = ["dev"]

[tool.ruff]
line-length = 100
